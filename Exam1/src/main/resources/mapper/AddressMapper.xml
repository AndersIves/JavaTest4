<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hand.exam1.mapper.AddressMapper">
    <select id="selectById" parameterType="java.lang.Long" resultType="com.hand.exam1.pojo.Address">
        select * from address where address_id = #{id};
    </select>
</mapper>